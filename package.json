{
    "scripts": {
        "beautifull": "lint-staged"
    },
    "lint-staged": {
        "*.{js,json,scss}": "prettier --write --single-quote --trailing-comma all --tab-width 4"
    },
    "devDependencies": {
        "lint-staged": "^15.1.0",
        "prettier": "^3.1.0"
    }
}
